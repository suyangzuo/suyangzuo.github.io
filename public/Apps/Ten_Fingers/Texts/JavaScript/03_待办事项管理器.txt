class SimpleTodoList { constructor() { this.todos = []; } addTodo(task) { this.todos.push({ id: Date.now(), task: task, completed: false }); } completeTodo(id) { const todo = this.todos.find(t => t.id === id); if (todo) todo.completed = true; } listTodos() { this.todos.forEach(todo => { const status = todo.completed ? '[DONE]' : '[PENDING]'; console.log(`${status} ${todo.task}`); }); } } const myTodoList = new SimpleTodoList(); myTodoList.addTodo('Learn JavaScript basics'); myTodoList.addTodo('Practice typing code'); myTodoList.completeTodo(myTodoList.todos[0].id); myTodoList.listTodos();